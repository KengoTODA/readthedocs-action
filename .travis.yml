dist: xenial
language: node_js
cache:
  directories:
    - ~/.npm
script:
    - npm run build
    - npm test
after_success:
  - npm run travis-deploy-once "npm run semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
