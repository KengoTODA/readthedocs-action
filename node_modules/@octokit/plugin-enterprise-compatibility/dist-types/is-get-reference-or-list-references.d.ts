import { EndpointOptions } from "@octokit/types";
export declare function isGetReference({ method, url }: EndpointOptions): boolean;
export declare function isListReferences({ method, url }: EndpointOptions): boolean;
